{
  "compilerOptions": {
    "composite": true,
    // Emit (disable if using tsup dts)
    "declaration": true,
    "declarationMap": true,
    // Node Environment
    "lib": ["ES2022"],
    "module": "ESNext",

    // Bundler Integration
    "moduleResolution": "Bundler",
    "target": "ES2022"
  },
  "extends": "./base.json"
}
